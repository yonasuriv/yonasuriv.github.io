{
    "type": "object",
    "description": "Azure ML Parse options settings",
    "required": [
        "delimiter",
        "skipLinesCount",
        "promoteHeaders"
    ],
    "properties": {
        "delimiter": {
            "type": "string",
            "description": "The character that separates each column in a row of data",
            "minLength": 1
        },
        "skipLinesCount": {
            "type": "number",
            "description": "The number of lines to skip at the beginning of the file(s)"
        },
        "promoteHeaders": {
            "type": "boolean",
            "description": "Whether or not to use the first line of the data file(s) as the header row"
        }
    }
}